// Firebase Web config is intentionally public configuration, not a password.
// Replace null with the config object from Firebase Console > Project settings > Your apps.
window.TRIP_FIREBASE_CONFIG = null;
window.TRIP_BOARD_ID = "tohoku-2026";
